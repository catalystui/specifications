<!-- 此翻译由 ChatGPT 生成，应由人工译者审核。 -->

# 在翻译经验证后，请在拉取请求中删除这些行。

# 认证详情

## C

### 概述

![Verified Indicator](https://www.catalystui.org/images/verification/verified-logo-generic.png)


C 编程语言截至 2026 年 7 月 7 日，已通过 FDEFSPEC（Rev. 1）和 FRELSPEC（Rev. 1）规范的认证。

#### 善意声明

我们基于善意相信，C 编程语言能够合理地表示以下规范中定义的概念与条款，并可用于实现符合这些规范的系统。

#### 审查说明

C 对基础数据表示、内存访问、文件、流、过程、函数、结构以及直接面向系统的实现提供了强大的底层支持。

尽管 C 并不原生提供若干面向对象的复合结构，但这些限制仅限于某些特定的 FRELSPEC 复合结构条款，并已在下文记录。这些失败项并不妨碍 C 在整体上被视为符合规范，但在使用 C 构建 CatalystUI 兼容实现时，应当理解这些限制。

#### 审查假设

本审查假定使用现代标准 C 支持；若存在依赖版本的特性，会在文中注明。精确宽度整数支持假定实现提供对应的 `<stdint.h>` 类型。

本审查将 C 视为一种低层表示语言。若未提供具名标准编解码器，则可通过字节数组、查找表和显式解析逻辑直接表示逐字节精确编码。

### 警告

* 精确宽度整数类型取决于实现支持。
* CP1252 可以逐字节表示，但未提供具名标准编解码器。
* UTF-16LE 序列化需要显式处理字节序。
* 某些 C 实现可能省略标准线程。
* 面向对象模式可以手动模拟，但并不是原生语言构造。

### 失败项

* C 不提供原生方法成员。
* C 不提供原生属性。
* C 不提供原生类。
* C 不提供原生接口。

### FDEFSPEC 认证

#### 数值类型

| 条目 | 已验证 | 说明 |
| --------- | -------- | ------------------------------------------- |
| Bit       | ⚠️       | 使用位字段或掩码。 |
| Nibble    | ⚠️       | 使用位字段或掩码。 |
| Byte      | ✅        | 通过 `unsigned char` 支持。 |
| Short     | ✅        | 通过 `int16_t` 和 `uint16_t` 支持。 |
| Int       | ✅        | 通过 `int32_t` 和 `uint32_t` 支持。 |
| Long      | ✅        | 通过 `int64_t` 和 `uint64_t` 支持。 |
| Float     | ✅        | 通过 `float` 支持。 |
| Double    | ✅        | 通过 `double` 支持。 |
| Boolean   | ✅        | 通过 `bool` 支持；C99+。 |

#### 文本编码

| 条目 | 已验证 | 说明 |
| --------- | -------- | --------------------------------------- |
| Codepoint | ✅        | 通过 `char32_t` 支持；C11+。 |
| ASCII     | ✅        | 可表示为字节值。 |
| CP1252    | ⚠️        | 需要显式字节映射。 |
| UTF-8     | ✅        | 通过 UTF-8 字面量支持；C11+。 |
| UTF-16LE  | ⚠️        | 需要处理字节序。 |

#### 操作状态

| 条目 | 已验证 | 说明 |
| --------- | -------- | ------------------------------- |
| Status    | ✅        | 可由 enum 表示。 |
| Context   | ✅        | 可由值表示。 |
| Operation | ✅        | 可由值表示。 |
| Detail    | ✅        | 可由值表示。 |
| Result    | ✅        | 可由 struct 表示。 |

### FRELSPEC 认证

#### 集合

| 条目 | 已验证 | 说明 |
| --------- | -------- | ------------------------------ |
| Set       | ✅        | 可由 struct 表示。 |
| Map       | ✅        | 可由 struct 表示。 |
| Array     | ✅        | 支持原生数组。 |
| File      | ✅        | 通过文件 API 支持。 |
| Stream    | ✅        | 通过流 API 支持。 |

#### 内存

| 条目 | 已验证 | 说明 |
| --------- | -------- | -------------------------- |
| Address   | ✅        | 支持地址。 |
| Pointer   | ✅        | 支持指针。 |
| Variable  | ✅        | 支持变量。 |
| Constant  | ✅        | 通过 `const` 支持。 |

#### 操作

| 条目 | 已验证 | 说明 |
| ----------- | -------- | --------------------------------------- |
| Instruction | ✅        | 通过机器操作表示。 |
| Procedure   | ✅        | 通过 `void` 函数支持。 |
| Function    | ✅        | 通过有返回值函数支持。 |

#### 线程

| 条目 | 已验证 | 说明 |
| ---------- | -------- | -------------------------------------- |
| Process    | ✅        | 通过宿主环境支持。 |
| Thread     | ⚠️        | 通过 `<threads.h>` 支持；C11+。 |
| Dispatcher | ✅        | 可以直接表示。 |

#### 复合结构

| 条目 | 已验证 | 说明 |
| --------- | -------- | ---------------------------------------- |
| Member    | ✅        | 支持结构成员。 |
| Object    | ✅        | struct 可形成可寻址复合体。 |
| Field     | ✅        | 支持结构字段。 |
| Method    | ❌        | 没有原生方法成员。 |
| Property  | ❌        | 没有显式 Get/Set 访问器映射。 |
| Structure | ✅        | 原生支持结构。 |
| Class     | ❌        | 没有原生类构造。 |
| Interface | ❌        | 没有原生接口构造。 |
