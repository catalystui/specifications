<!-- 此翻译由 ChatGPT 生成，应由人工译者审核。 -->

# 在翻译经验证后，请在拉取请求中删除这些行。

# 认证详情

## C++

### 概述

![Verified Indicator](https://www.catalystui.org/images/verification/verified-logo-generic.png)


C++ 编程语言截至 2026 年 7 月 7 日，已通过 FDEFSPEC（Rev. 1）和 FRELSPEC（Rev. 1）规范的认证。

#### 善意声明

我们基于善意相信，C++ 编程语言能够合理地表示以下规范中定义的概念与条款，并可用于实现符合这些规范的系统。

#### 审查假设

本审查假定使用现代标准 C++ 支持；若存在依赖版本的特性，会在文中注明。精确宽度整数支持假定实现提供对应的 `<cstdint>` 类型。

本审查将 C++ 视为一种系统编程语言，它直接支持底层表示、对象建模、内存控制、泛型编程和并发执行。

### 警告

* 精确宽度整数类型取决于实现支持。
* CP1252 可以逐字节表示，但未提供具名标准编解码器。
* UTF-16LE 序列化需要显式处理字节序。
* 某些并发和字符特性需要较新的 C++ 版本。

### 失败项

* 在本次认证中，未发现已知的 FDEFSPEC 或 FRELSPEC 失败项。

### FDEFSPEC 认证

#### 数值类型

| 条目 | 已验证 | 说明 |
| --------- | -------- | ------------------------------------------- |
| Bit       | ⚠️       | 使用位字段或掩码。 |
| Nibble    | ⚠️       | 使用位字段或掩码。 |
| Byte      | ✅        | 通过 `std::byte` 支持。 |
| Short     | ✅        | 通过 `int16_t` 和 `uint16_t` 支持。 |
| Int       | ✅        | 通过 `int32_t` 和 `uint32_t` 支持。 |
| Long      | ✅        | 通过 `int64_t` 和 `uint64_t` 支持。 |
| Float     | ✅        | 通过 `float` 支持。 |
| Double    | ✅        | 通过 `double` 支持。 |
| Boolean   | ✅        | 通过 `bool` 支持。 |

#### 文本编码

| 条目 | 已验证 | 说明 |
| --------- | -------- | ------------------------------------ |
| Codepoint | ✅        | 通过 `char32_t` 支持。 |
| ASCII     | ✅        | 可表示为字节值。 |
| CP1252    | ⚠️        | 需要显式字节映射。 |
| UTF-8     | ✅        | 通过 `char8_t` 支持；C++20+。 |
| UTF-16LE  | ⚠️        | 需要处理字节序。 |

#### 操作状态

| 条目 | 已验证 | 说明 |
| --------- | -------- | ---------------------------------------- |
| Status    | ✅        | 可由 enum 表示。 |
| Context   | ✅        | 可由值表示。 |
| Operation | ✅        | 可由值表示。 |
| Detail    | ✅        | 可由值表示。 |
| Result    | ✅        | 可由 struct 或 class 表示。 |

### FRELSPEC 认证

#### 集合

| 条目 | 已验证 | 说明 |
| --------- | -------- | ------------------------------------------ |
| Set       | ✅        | 通过 `std::set` 支持。 |
| Map       | ✅        | 通过 `std::map` 支持。 |
| Array     | ✅        | 通过数组和 `std::array` 支持。 |
| File      | ✅        | 通过文件流支持。 |
| Stream    | ✅        | 通过 iostream 支持。 |

#### 内存

| 条目 | 已验证 | 说明 |
| --------- | -------- | ------------------------------------------ |
| Address   | ✅        | 支持地址。 |
| Pointer   | ✅        | 支持指针。 |
| Variable  | ✅        | 支持变量。 |
| Constant  | ✅        | 通过 `const` 和 `constexpr` 支持。 |

#### 操作

| 条目 | 已验证 | 说明 |
| ----------- | -------- | --------------------------------------- |
| Instruction | ✅        | 通过机器操作表示。 |
| Procedure   | ✅        | 通过 `void` 函数支持。 |
| Function    | ✅        | 通过有返回值函数支持。 |

#### 线程

| 条目 | 已验证 | 说明 |
| ---------- | -------- | -------------------------------------- |
| Process    | ✅        | 通过宿主环境支持。 |
| Thread     | ✅        | 通过 `std::thread` 支持。 |
| Dispatcher | ✅        | 通过调度与执行框架支持。 |

#### 复合结构

| 条目 | 已验证 | 说明 |
| --------- | -------- | ---------------------------------------- |
| Member    | ✅        | 支持成员。 |
| Object    | ✅        | 支持对象。 |
| Field     | ✅        | 支持字段。 |
| Method    | ✅        | 支持方法。 |
| Property  | ✅        | 可通过访问器模式表示。 |
| Structure | ✅        | 原生支持结构。 |
| Class     | ✅        | 支持类。 |
| Interface | ✅        | 可通过抽象类表示。 |
