<!-- این ترجمه توسط ChatGPT تولید شده و باید توسط مترجم انسانی بررسی شود. -->
<!-- پس از تأیید ترجمه، این خطوط را در یک pull request حذف کنید. -->

# جزئیات تأیید

## C#

### نمای کلی

![Verified Indicator](https://www.catalystui.org/images/verification/verified-logo-generic.png)


زبان برنامه‌نویسی C# در برابر مشخصات FDEFSPEC (Rev. 1) و FRELSPEC (Rev. 1) تا تاریخ ۷ ژوئیهٔ ۲۰۲۶ تأیید شده است.

#### بیانیهٔ حسن نیت

ما با حسن نیت باور داریم که زبان برنامه‌نویسی C# می‌تواند مفاهیم و provisionهای تعریف‌شده در مشخصات زیر را به‌صورت معقول نمایش دهد و می‌تواند برای پیاده‌سازی سیستم‌هایی استفاده شود که با این مشخصات سازگار هستند.

#### فرض‌های بازبینی

این بازبینی پشتیبانی مدرن C# و .NET را فرض می‌کند، جایی که قابلیت‌های وابسته به نسخه مشخص شده‌اند.

### هشدارها

* پشتیبانی CP1252 از طریق provider رسمی code pages در .NET در دسترس است، اما بسته به runtime هدف ممکن است به ثبت provider یا یک بستهٔ اضافی نیاز داشته باشد.
* رفتار unsafe pointer ممکن است به مجوز unsafe صریح نیاز داشته باشد.

### شکست‌ها

* در جریان این تأیید، هیچ شکست شناخته‌شدهٔ FDEFSPEC یا FRELSPEC یافت نشد.

### تأیید FDEFSPEC

#### اعداد

| Provision | Verified | Notes                                   |
| --------- | -------- | --------------------------------------- |
| Bit       | ⚠️       | نوع عددی ۱ بیتی وجود ندارد؛ widen کنید.                |
| Nibble    | ⚠️       | نوع عددی ۴ بیتی وجود ندارد؛ widen کنید.                |
| Byte      | ✅        | از طریق `byte` و `sbyte` پشتیبانی می‌شود.   |
| Short     | ✅        | از طریق `short` و `ushort` پشتیبانی می‌شود. |
| Int       | ✅        | از طریق `int` و `uint` پشتیبانی می‌شود.     |
| Long      | ✅        | از طریق `long` و `ulong` پشتیبانی می‌شود.   |
| Float     | ✅        | از طریق `float` پشتیبانی می‌شود.              |
| Double    | ✅        | از طریق `double` پشتیبانی می‌شود.             |
| Boolean   | ✅        | از طریق `bool` پشتیبانی می‌شود.               |

#### رمزگذاری متن

| Provision | Verified | Notes                                  |
| --------- | -------- | -------------------------------------- |
| Codepoint | ✅        | از طریق اعداد و `Rune` پشتیبانی می‌شود. |
| ASCII     | ✅        | از طریق `Encoding.ASCII` پشتیبانی می‌شود.    |
| CP1252    | ⚠️       | به provider مربوط به code pages نیاز دارد.          |
| UTF-8     | ✅        | از طریق `Encoding.UTF8` پشتیبانی می‌شود.     |
| UTF-16LE  | ✅        | از طریق `Encoding.Unicode` پشتیبانی می‌شود.  |

#### وضعیت عملیات

| Provision | Verified | Notes                                       |
| --------- | -------- | ------------------------------------------- |
| Status    | ✅        | می‌تواند با یک نوع سفارشی نمایش داده شود.        |
| Context   | ✅        | می‌تواند با یک مقدار سفارشی نمایش داده شود.       |
| Operation | ✅        | می‌تواند با یک مقدار سفارشی نمایش داده شود.       |
| Detail    | ✅        | می‌تواند با یک مقدار سفارشی نمایش داده شود.       |
| Result    | ✅        | می‌تواند با یک نوع بازگشتی سفارشی نمایش داده شود. |

### تأیید FRELSPEC

#### مجموعه‌ها

| Provision | Verified | Notes                                        |
| --------- | -------- | -------------------------------------------- |
| Set       | ✅        | از طریق `HashSet<T>` پشتیبانی می‌شود.              |
| Map       | ✅        | از طریق `Dictionary<TKey,TValue>` پشتیبانی می‌شود. |
| Array     | ✅        | arrayهای native پشتیبانی می‌شوند.                 |
| File      | ✅        | از طریق file APIها پشتیبانی می‌شود.                 |
| Stream    | ✅        | از طریق `Stream` پشتیبانی می‌شود.                  |

#### حافظه

| Provision | Verified | Notes                                      |
| --------- | -------- | ------------------------------------------ |
| Address   | ✅        | از طریق referenceها و pointerها پشتیبانی می‌شود. |
| Pointer   | ✅        | از طریق unsafe pointerها پشتیبانی می‌شود.         |
| Variable  | ✅        | variableها پشتیبانی می‌شوند.                   |
| Constant  | ✅        | از طریق `const` و `readonly` پشتیبانی می‌شود.  |

#### عملیات

| Provision   | Verified | Notes                                  |
| ----------- | -------- | -------------------------------------- |
| Instruction | ✅        | از طریق IL و operationها نمایش داده می‌شود. |
| Procedure   | ✅        | از طریق methodهای `void` پشتیبانی می‌شود.      |
| Function    | ✅        | از طریق methodهای بازگشتی پشتیبانی می‌شود.   |

#### Threading

| Provision  | Verified | Notes                                             |
| ---------- | -------- | ------------------------------------------------- |
| Process    | ✅        | از طریق برنامه و `Process` APIها پشتیبانی می‌شود. |
| Thread     | ✅        | از طریق `Thread` پشتیبانی می‌شود.                       |
| Dispatcher | ✅        | از طریق taskها و schedulerها پشتیبانی می‌شود.           |

#### ترکیب‌ها

| Provision | Verified | Notes                              |
| --------- | -------- | ---------------------------------- |
| Member    | ✅        | memberهای type پشتیبانی می‌شوند.        |
| Object    | ✅        | objectها پشتیبانی می‌شوند.             |
| Field     | ✅        | fieldها پشتیبانی می‌شوند.              |
| Method    | ✅        | methodها پشتیبانی می‌شوند.             |
| Property  | ✅        | propertyها به‌صورت native پشتیبانی می‌شوند. |
| Structure | ✅        | structureها به‌صورت native پشتیبانی می‌شوند. |
| Class     | ✅        | classها پشتیبانی می‌شوند.             |
| Interface | ✅        | interfaceها پشتیبانی می‌شوند.          |
