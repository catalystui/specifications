<!-- 此翻译由 ChatGPT 生成，应由人工译者审核。 -->

# 在翻译经验证后，请在拉取请求中删除这些行。

# 认证详情

## Java

### 概述

![Verified Indicator](https://www.catalystui.org/images/verification/verified-logo-generic.png)


Java 编程语言截至 2026 年 7 月 7 日，已通过 FDEFSPEC（Rev. 1）和 FRELSPEC（Rev. 1）规范的认证。

#### 善意声明

我们基于善意相信，Java 编程语言能够合理地表示以下规范中定义的大部分概念与条款，并可用于实现符合这些规范的系统。

#### 审查假设

本审查假定使用现代 Java 语言支持；若存在依赖版本的特性，会在文中注明。诸如局部变量类型推断、record 以及 foreign memory access 等特性可能需要较新的 Java 版本。

### 警告

* Java 不原生支持无符号数值类型，因此某些条款可能需要扩宽或替代表示。
* 直接内存地址与指针式行为可能需要 Java 22+ Foreign Function and Memory API 支持。
* Java 不具备原生属性语法，因此需要使用 getter 和 setter 方法。

### 失败项

* Java 不将 CP1252 作为受保证的标准字符集提供；要实现完全符合，可能需要特定实现支持、额外库或自定义处理。
* Java 属性并不提供由必需的 Get/Set 访问器映射支持的显式键控成员。

### FDEFSPEC 认证

#### 数值类型

| 条目 | 已验证 | 说明 |
| --------- | -------- | ------------------------------------------------ |
| Bit       | ⚠️       | 没有 1 位数值类型；需扩宽。 |
| Nibble    | ⚠️       | 没有 4 位数值类型；需扩宽。 |
| Byte      | ⚠️       | 仅支持有符号 8 位；无符号需扩宽。 |
| Short     | ⚠️       | 仅支持有符号 16 位；无符号需扩宽。 |
| Int       | ⚠️       | 有符号 32 位；无符号辅助需要 Java 8+。 |
| Long      | ⚠️       | 有符号 64 位；无符号辅助需要 Java 8+。 |
| Float     | ✅        | 支持 32 位浮点。 |
| Double    | ✅        | 支持 64 位浮点。 |
| Boolean   | ✅        | 支持布尔类型。 |

#### 文本编码

| 条目 | 已验证 | 说明 |
| --------- | -------- | ---------------------------------------- |
| Codepoint | ✅        | 通过 `int` 和 `Character` 支持。 |
| ASCII     | ✅        | 保证的标准字符集。 |
| CP1252    | ❌        | `StandardCharsets` 不保证提供。 |
| UTF-8     | ✅        | 保证的标准字符集。 |
| UTF-16LE  | ✅        | 保证的标准字符集。 |

#### 操作状态

| 条目 | 已验证 | 说明 |
| --------- | -------- | ------------------------------------------- |
| Status    | ✅        | 可由自定义类型表示。 |
| Context   | ✅        | 可由自定义值表示。 |
| Operation | ✅        | 可由自定义值表示。 |
| Detail    | ✅        | 可由自定义值表示。 |
| Result    | ✅        | 可由自定义返回类型表示。 |

### FRELSPEC 认证

#### 集合

| 条目 | 已验证 | 说明 |
| --------- | -------- | ------------------------------ |
| Set       | ✅        | 通过 `Set` 支持。 |
| Map       | ✅        | 通过 `Map` 支持。 |
| Array     | ✅        | 原生数组受支持。 |
| File      | ✅        | 通过文件 API 支持。 |
| Stream    | ✅        | 通过流 API 支持。 |

#### 内存

| 条目 | 已验证 | 说明 |
| --------- | -------- | -------------------------------------------------------------- |
| Address   | ⚠️       | 直接地址需要 Java 22+ FFM。 |
| Pointer   | ⚠️       | 类指针访问需要 Java 22+ FFM。 |
| Variable  | ✅        | 支持声明和 `var`；`var` 需要 Java 10+。 |
| Constant  | ✅        | 通过 `final` 支持。 |

#### 操作

| 条目 | 已验证 | 说明 |
| ----------- | -------- | -------------------------------------------- |
| Instruction | ✅        | 通过字节码与操作表示。 |
| Procedure   | ✅        | 通过 `void` 方法支持。 |
| Function    | ✅        | 通过返回值方法支持。 |

#### 线程

| 条目 | 已验证 | 说明 |
| ---------- | -------- | ------------------------------------------------- |
| Process    | ✅        | 通过应用程序和 `Process` API 支持。 |
| Thread     | ✅        | 通过 `Thread` 支持。 |
| Dispatcher | ✅        | 通过 `Executor` API 支持。 |

#### 复合结构

| 条目 | 已验证 | 说明 |
| --------- | -------- | -------------------------------------- |
| Member    | ✅        | 支持类成员。 |
| Object    | ✅        | 支持对象。 |
| Field     | ✅        | 支持字段。 |
| Method    | ✅        | 支持方法。 |
| Property  | ❌        | 没有显式访问器映射。 |
| Structure | ✅        | 通过 record 支持；Java 16+。 |
| Class     | ✅        | 支持类。 |
| Interface | ✅        | 支持接口。 |
