<!-- 此翻译由 ChatGPT 生成，应由人工译者审核。 -->

# 在翻译经验证后，请在拉取请求中删除这些行。

# 认证详情

## C#

### 概述

![Verified Indicator](https://www.catalystui.org/images/verification/verified-logo-generic.png)


C# 编程语言截至 2026 年 7 月 7 日，已通过 FDEFSPEC（Rev. 1）和 FRELSPEC（Rev. 1）规范的认证。

#### 善意声明

我们基于善意相信，C# 编程语言能够合理地表示以下规范中定义的概念与条款，并可用于实现符合这些规范的系统。

#### 审查假设

本审查假定使用现代 C# 与 .NET 支持；若存在依赖版本的特性，会在文中注明。

### 警告

* CP1252 支持可通过官方 .NET code pages provider 获得，但根据目标运行时不同，可能需要注册 provider 或安装额外包。
* 非安全指针行为可能需要显式启用 unsafe 授权。

### 失败项

* 在本次认证中，未发现已知的 FDEFSPEC 或 FRELSPEC 失败项。

### FDEFSPEC 认证

#### 数值类型

| 条目 | 已验证 | 说明 |
| --------- | -------- | --------------------------------------- |
| Bit       | ⚠️       | 没有 1 位数值类型；需扩宽。 |
| Nibble    | ⚠️       | 没有 4 位数值类型；需扩宽。 |
| Byte      | ✅        | 通过 `byte` 和 `sbyte` 支持。 |
| Short     | ✅        | 通过 `short` 和 `ushort` 支持。 |
| Int       | ✅        | 通过 `int` 和 `uint` 支持。 |
| Long      | ✅        | 通过 `long` 和 `ulong` 支持。 |
| Float     | ✅        | 通过 `float` 支持。 |
| Double    | ✅        | 通过 `double` 支持。 |
| Boolean   | ✅        | 通过 `bool` 支持。 |

#### 文本编码

| 条目 | 已验证 | 说明 |
| --------- | -------- | -------------------------------------- |
| Codepoint | ✅        | 通过数值类型和 `Rune` 支持。 |
| ASCII     | ✅        | 通过 `Encoding.ASCII` 支持。 |
| CP1252    | ⚠️       | 需要 code pages provider。 |
| UTF-8     | ✅        | 通过 `Encoding.UTF8` 支持。 |
| UTF-16LE  | ✅        | 通过 `Encoding.Unicode` 支持。 |

#### 操作状态

| 条目 | 已验证 | 说明 |
| --------- | -------- | ------------------------------------------- |
| Status    | ✅        | 可由自定义类型表示。 |
| Context   | ✅        | 可由自定义值表示。 |
| Operation | ✅        | 可由自定义值表示。 |
| Detail    | ✅        | 可由自定义值表示。 |
| Result    | ✅        | 可由自定义返回类型表示。 |

### FRELSPEC 认证

#### 集合

| 条目 | 已验证 | 说明 |
| --------- | -------- | -------------------------------------------- |
| Set       | ✅        | 通过 `HashSet<T>` 支持。 |
| Map       | ✅        | 通过 `Dictionary<TKey,TValue>` 支持。 |
| Array     | ✅        | 原生数组受支持。 |
| File      | ✅        | 通过文件 API 支持。 |
| Stream    | ✅        | 通过 `Stream` 支持。 |

#### 内存

| 条目 | 已验证 | 说明 |
| --------- | -------- | ------------------------------------------ |
| Address   | ✅        | 通过引用和指针支持。 |
| Pointer   | ✅        | 通过 unsafe 指针支持。 |
| Variable  | ✅        | 变量受支持。 |
| Constant  | ✅        | 通过 `const` 和 `readonly` 支持。 |

#### 操作

| 条目 | 已验证 | 说明 |
| ----------- | -------- | -------------------------------------- |
| Instruction | ✅        | 通过 IL 与操作表示。 |
| Procedure   | ✅        | 通过 `void` 方法支持。 |
| Function    | ✅        | 通过返回值方法支持。 |

#### 线程

| 条目 | 已验证 | 说明 |
| ---------- | -------- | ------------------------------------------------- |
| Process    | ✅        | 通过应用程序和 `Process` API 支持。 |
| Thread     | ✅        | 通过 `Thread` 支持。 |
| Dispatcher | ✅        | 通过任务与调度器支持。 |

#### 复合结构

| 条目 | 已验证 | 说明 |
| --------- | -------- | ---------------------------------- |
| Member    | ✅        | 类型成员受支持。 |
| Object    | ✅        | 对象受支持。 |
| Field     | ✅        | 字段受支持。 |
| Method    | ✅        | 方法受支持。 |
| Property  | ✅        | 属性原生受支持。 |
| Structure | ✅        | 结构原生受支持。 |
| Class     | ✅        | 类受支持。 |
| Interface | ✅        | 接口受支持。 |
